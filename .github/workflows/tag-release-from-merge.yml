name: Tag Release From Merge

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  tag-release:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.TIDAS_RELEASE_AUTOMATION_TOKEN }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install stable Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install docpact
        run: cargo install docpact --version 0.1.9 --force

      - name: Validate automation token
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "error: missing TIDAS_RELEASE_AUTOMATION_TOKEN secret" >&2
            exit 1
          fi

      - name: Run docpact release gate before tag
        env:
          BASE_REF: ${{ github.event.before }}
          HEAD_REF: ${{ github.sha }}
        run: |
          set -euo pipefail
          if [ "$BASE_REF" = "0000000000000000000000000000000000000000" ]; then
            BASE_REF="${HEAD_REF}^"
          fi
          scripts/docpact-gate.sh --base "$BASE_REF" --head "$HEAD_REF"

      - name: Run automation contract regressions
        run: python3 ./scripts/ci/test-automation-contracts.py

      - name: Detect release version changes
        id: release
        env:
          BASE_REF: ${{ github.event.before }}
          HEAD_REF: ${{ github.sha }}
        run: |
          python3 ./scripts/ci/detect-release-changes.py \
            --base-ref "$BASE_REF" \
            --head-ref "$HEAD_REF"

      - name: Stop when no package version changed
        if: steps.release.outputs.any_changed != 'true'
        run: echo "No package version changes detected on main."

      - name: Set up Node.js
        if: steps.release.outputs.typescript_changed == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: sdks/typescript/package-lock.json

      - name: Upgrade npm for release parity
        if: steps.release.outputs.typescript_changed == 'true'
        run: npm install --global npm@11.5.1

      - name: Verify TypeScript package before release tag
        if: steps.release.outputs.typescript_changed == 'true'
        run: ./scripts/ci/verify-typescript-package.sh

      - name: Set up Python
        if: steps.release.outputs.python_changed == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up uv
        if: steps.release.outputs.python_changed == 'true'
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Verify Python package before release tag
        if: steps.release.outputs.python_changed == 'true'
        run: ./scripts/ci/verify-python-package.sh

      - name: Check TypeScript registry version availability
        if: steps.release.outputs.typescript_changed == 'true'
        env:
          TYPESCRIPT_VERSION: ${{ steps.release.outputs.typescript_version }}
        run: |
          python3 ./scripts/ci/release-version.py assert-unpublished \
            --package typescript \
            --version "$TYPESCRIPT_VERSION"

      - name: Check Python registry version availability
        if: steps.release.outputs.python_changed == 'true'
        env:
          PYTHON_VERSION: ${{ steps.release.outputs.python_version }}
        run: |
          python3 ./scripts/ci/release-version.py assert-unpublished \
            --package python \
            --version "$PYTHON_VERSION"

      - name: Create TypeScript release tag
        if: steps.release.outputs.typescript_changed == 'true'
        env:
          TAG_NAME: ${{ steps.release.outputs.typescript_tag }}
          TARGET_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          if gh api "repos/${GITHUB_REPOSITORY}/git/ref/tags/${TAG_NAME}" >/dev/null 2>&1; then
            echo "error: tag ${TAG_NAME} already exists" >&2
            exit 1
          fi
          gh api "repos/${GITHUB_REPOSITORY}/git/refs" \
            --method POST \
            -f ref="refs/tags/${TAG_NAME}" \
            -f sha="${TARGET_SHA}"

      - name: Create Python release tag
        if: steps.release.outputs.python_changed == 'true'
        env:
          TAG_NAME: ${{ steps.release.outputs.python_tag }}
          TARGET_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          if gh api "repos/${GITHUB_REPOSITORY}/git/ref/tags/${TAG_NAME}" >/dev/null 2>&1; then
            echo "error: tag ${TAG_NAME} already exists" >&2
            exit 1
          fi
          gh api "repos/${GITHUB_REPOSITORY}/git/refs" \
            --method POST \
            -f ref="refs/tags/${TAG_NAME}" \
            -f sha="${TARGET_SHA}"
